.group__block
  - current_user.groups.each do |group|
    =link_to group_messages_path(current_user.id) do #each
      %p.group__block__title
        = group.name
      %p.group__block__text
        test
